<!DOCTYPE html>
<html lang="zh-CN">
    <head>
        <meta charset="utf-8">
            <meta http-equiv="X-UA-Compatible" content="IE=edge">
                <meta name="viewport" content="width=device-width, initial-scale=1">
                    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
                    <title>Slave-Yo</title>
                    
                    <!-- Bootstrap -->
                    <link href="/style/bootstrap.min.css" rel="stylesheet">
                        
                        <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
                        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
                        <!--[if lt IE 9]>
                         <script src="https://cdn.bootcss.com/html5shiv/3.7.3/html5shiv.min.js"></script>
                         <script src="https://cdn.bootcss.com/respond.js/1.4.2/respond.min.js"></script>
                         <![endif]-->
                        </head>
    <body>
        <h1>Slave</h1>
        <p class="lead">Don't repeat yourself. Let Slave handle this kind of stuff.</p>
        
        <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
        <script src="https://cdn.bootcss.com/jquery/1.12.4/jquery.min.js"></script>
        <!-- Include all compiled plugins (below), or include individual files as needed -->
        <script src="/js/bootstrap.min.js"></script>
        
        <!-- Form -->
        <form id="orderForm" class="form-inline" action="/v1/order" method="post">
            <div id="customId">
            <div class="form-group">
                <label for="inputVaribleName">Name</label>
                <input type="text" name="variableName" class="form-control" placeholder="e.g.,titleLabel"/>
            </div>
            <div class="form-group">
                <label for="inputVaribleType">Type</label>
                <select class="form-control" name="variableType">
                    <option value="UILabel">UILabel</option>
                    <option value="UIButton">UIButton</option>
                    <option value="UITableView">UITableView</option>
                    <option value="UIView">UIView</option>
                    <option value="UIControl">UIControl</option>
                </select>
            </div>
            </div>
            
            <div class="form-group">
            <input class="button-primary u-full-width" type="button" value="+" onClick="doAddFunction();"/>
            </div>
            
            <div class="form-group">
                <input class="button-primary u-full-width" type="button" value="Generate" onClick="sendPost();"/>
            </div>
        </form>
        
        <script>
            function doAddFunction() {
                var div = document.getElementById("customId");
                var newDiv = div.cloneNode(true)
                document.getElementById("orderForm").appendChild(newDiv);
            }
        
            function sendPost() {
                
            }
        </script>
        
        <!-- <input id="clickMe" type="button" value="clickMe" onClick="doFunction();"/>
        <script>
            function doFunction() {
                window.alert('hello');
            }
        </script>
        -->
         
    </body>
</html>
